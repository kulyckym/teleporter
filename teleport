--This code will be used as a money machine that when the players want to teleport, they pay and the doors open so that they go--
local teleportticket = 4
local userprice = 5
if userprice < teleportticket then
-- the gate will stay closed.
print("you are missing " .. teleportticket-userprice .. " dollars")
rs.setOutput("left", false)
 
-- gate will open to alow teleportee
   elseif userprice >= teleportticket then
  rs.setOutput("left", true)
  sleep(5)
  rs.setOutput("left", false)
  print("enjoy the ride")
end

